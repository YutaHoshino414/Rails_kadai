<%= form_with(model: @tsubuyaki, local: true) do |form| %>
 
  <div class="tsubuyaki_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>